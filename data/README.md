# Data Directory

- `crawler_output.jsonl` — Cleaned text and metadata from Phase 1 (Europeana, `LANGUAGE:en`, France/Paris-themed queries in `config.py`)
- `extracted_knowledge.csv` — Entities (entity, type, source_url)
- `extracted_triples.csv` — Subject-predicate-object triples
- `kb_initial.ttl` — Initial RDF KB before expansion
- `samples/` — Sample data (if provided)
